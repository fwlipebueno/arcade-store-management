<a id="english"></a>

<p align="right">
  <kbd><a href="#english">English</a></kbd>
  <kbd><a href="#portuguese">Português</a></kbd>
  <kbd><a href="../README.md#english">Main README</a></kbd>
</p>

# Frontend
Initial interface for the Arcade Store Management system.

It uses plain HTML, CSS and JavaScript to keep the project lightweight while the course still evolves database and integration topics. The application consumes the backend API at `http://localhost:8080/api`.

## Running
With the backend running, serve this folder with a simple static server. Example:

```bash
python -m http.server 5500
```

Then open:

```text
http://localhost:5500
```

When a final frontend approach is defined, this folder can evolve into a build-based application without changing the backend domain or services.

---

<a id="portuguese"></a>

<p align="right">
  <kbd><a href="#english">English</a></kbd>
  <kbd><a href="#portuguese">Português</a></kbd>
  <kbd><a href="../README.md#portuguese">README principal</a></kbd>
</p>

# Frontend
Interface inicial do sistema Arcade Store Management.

Ela usa HTML, CSS e JavaScript puro para manter o projeto leve enquanto a disciplina ainda evolui os pontos de banco de dados e integração. A aplicação consome a API do backend em `http://localhost:8080/api`.

## Execução
Com o backend em execução, sirva esta pasta com um servidor estático simples. Exemplo:

```bash
python -m http.server 5500
```

Depois acesse:

```text
http://localhost:5500
```

Quando a abordagem definitiva do frontend for definida, esta pasta pode evoluir para uma aplicação com build próprio sem alterar o domínio e os services do backend.
